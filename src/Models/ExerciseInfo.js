export default class ExerciseInfo {
    constructor(name, sets, reps, weight, max, multiplier) {
        this.name = name;
        this.reps = reps;
        this.sets = sets;
        this.weight = weight;
        this.max = max;
        this.multiplier = multiplier;
    }
}