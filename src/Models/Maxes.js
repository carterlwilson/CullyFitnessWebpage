export default class Maxes {
    constructor(benchpress, squat, deadlift, overheadpress) {
        this.benchpress = benchpress;
        this.squat = squat;
        this.deadlift = deadlift;
        this.overheadpress = overheadpress;
    }
}